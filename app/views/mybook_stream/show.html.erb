<%=@readedbook.book_name%>

<%= form_tag '/mybook_stream/saveidea', :method=>:post do%>

	<%= text_field :idea, :book_id%>
        <%= text_field :idea, :book_id, :value=>@id%>

	<%= text_field :idea, :user_id%>
        <%= text_field :idea, :user_id, :value=>current_user.id%>

	<%= text_field :idea, :process%>
        <%= text_field :idea, :process, :value=>20%>

        <%= text_field :idea, :idea%>
        <%= text_field :idea, :idea, :value=>"enter idea here"%>
         <%= submit_tag 'submit'%>
<% end%>
